<div class="modal" tabindex="-1" role="dialog" id="modal">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <div class="container">
          <h5 class="modal-title">Naissance</h5>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
              <span aria-hidden="true">&times;</span>
            </button>
          </div>
        </div>
        <div class="modal-body">
          <div class="form-popup">
              <%= simple_form_for @baby do |f| %>
                <%= f.input :first_name, label: "Prénom" %>
                <%= f.input :last_name,  label: "Nom de Famille" %>
                <%= f.input :birth_date,  label: "Date et heure de naissance"  %>
                <%= f.input :diagnostic,  label: "Diagnostique"  %>
                <%= f.input :weight,  label: "Poids"  %>
                <%= f.input :heigh,  label: "Taille"  %>
                <%= f.input :head_circumference,  label: "Périmètre crânien"  %>
                <%= f.input :sex, collection: Baby::SEX ,label: "Sexe"  %>
                <%= f.input :monitoring,  label: "Monitorage"  %>
                <%= f.input :monitoring_options, collection: Baby::MONITORINGOPTIONS, label: "Types de monitorage (utiliser ctrl ou cmd pour une sélection multiple)", input_html: { multiple: true }  %>
                <%= f.input :intensivecare,  label: "Réanimation"  %>
                <%= f.input :intensivecare_options, collection: Baby::INTENSIVECAREOPTIONS, label: "Actes de réanimation (utiliser ctrl ou cmd pour une sélection multiple)", input_html: { multiple: true }  %>
                <%= f.input :malformation,  label: "Malformation"   %>
                <%= f.input :infectiouscontext,  label: "Infections"  %>
                <%= f.input :infectiouscontext_options, collection: Baby::INFECTIOUSCONTEXTOPTIONS, label: "Types d'infections (utiliser ctrl ou cmd pour une sélection multiple)", input_html: { multiple: true }  %>
                <%= f.input :exit_room, collection: Baby::EXITROOM, label: "Exit room"  %>
                <%= f.input :breastfeeding, collection: Baby::BREASTFEEDING, label: "Allaitement"  %>
                <%= f.input :pathologies, collection: Baby::PATHOLOGIES, label: "Pathologies (utiliser ctrl ou cmd pour une sélection multiple)", input_html: { multiple: true }  %>
                <%= f.submit 'Enregistrer naissance 👶' %>
              <% end %>
          </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-dismiss="modal">Fermer</button>
          <!-- <button type="button" class="btn btn-primary">Enregistrer</button> -->
        </div>
    </div>
  </div>
</div>













